name: Deploy to VPS

on:
    push:
        branches:
            - main # Runs every time you push to the 'main' branch

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout code
              uses: actions/checkout@v4

            - name: Execute remote SSH commands
              uses: appleboy/ssh-action@v1.0.3
              with:
                  host: ${{ secrets.REMOTE_HOST }}
                  username: ${{ secrets.REMOTE_USER }}
                  key: ${{ secrets.SSH_PRIVATE_KEY }}
                  script: |
                      set -e

                      cd investor-portal
                      git remote set-url origin https://${{ secrets.GH_PAT }}@github.com/${{ github.repository }}.git || true
                      git pull origin main

                      # Build new images while old containers are still running
                      docker compose build --no-cache

                      # Quick swap: stop old containers and start new ones (seconds of downtime)
                      docker compose up -d --force-recreate

                      # Clean up old images after deployment
                      docker image prune -f

                      # Show status
                      sleep 3
                      docker compose ps
                      docker compose logs --tail=30
