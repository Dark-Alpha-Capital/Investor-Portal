// This file is no longer needed since DealsMarketplace now fetches data client-side
// Keeping as a placeholder for backwards compatibility

import { DealsMarketplace } from "./deals-marketplace";

export async function DealsDataFetcher() {
  return <DealsMarketplace />;
}
