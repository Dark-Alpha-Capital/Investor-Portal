{
  "name": "worker",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run index.ts --watch",
    "build": "echo 'No build needed for Bun runtime'",
    "lint": "eslint . || true",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@repo/mail": "*",
    "axios": "^1.13.2",
    "bullmq": "^5.66.2",
    "ioredis": "^5.8.2",
    "slugify": "^1.6.6",
    "wav": "^1.0.2",
    "webdav": "^5.8.0"
  }
}
